---
import ImageMod from "@/components/ImageMod.astro";
import Accordion from "@/functional-components/Accordion";
import Testimonials from "@/functional-components/Testimonials";
import Base from "@/layouts/Base.astro";
import { markdownify } from "@/lib/utils/textConverter";
import PageHeader from "@/partials/PageHeader.astro";
import type { CollectionEntry } from "astro:content";
import { getEntry } from "astro:content";
import { FaBoxOpen, FaHeadset, FaRegCheckCircle } from "react-icons/fa";
const about = (await getEntry("about", "-index")) as CollectionEntry<"about">;
import prueba from "./resources/True_face_banner_vertical.png";

const {
  title,
  meta_title,
  description,
  image,
  about_us,
  faq_section_title,
  button,
  faq_section_subtitle,
  faqs,
  testimonials_section_enable,
  testimonials_section_title,
  testimonials,
  staff_section_enable,
  staff,
} = about.data;
---

<Base
  title={title}
  meta_title={meta_title}
  description={description}
  image={image}
>
  <PageHeader title={title} />

  <section>
    <div class="container">
      {
        about_us?.map((section, index) => (
          <div class={`lg:flex gap-8 mt-14 lg:mt-28`}>
            {index % 2 === 0 ? (
              <>

                <div class="mt-10 lg:mt-0">
                  <h2>Nuestra esencia</h2>
                  <p><b style="font-size: 25px;">Misión: </b>Ofrecer servicios y productos de alta calidad que reflejen la esencia del estilo urbano moderno, generando confianza y apoyando la identidad en cada cliente.</p>
                  <p><b style="font-size: 25px;">Visión: </b>Ser una marca reconocida a nivel nacional e internacional por revolucionar el concepto del estilo urbano, combinando precisión, autenticidad y evolución constante.</p>
                  <p><b style="font-size: 25px;">Valores: </b>Autenticidad, Innovación, Confianza, Comunidad.</p>

                </div>
              </>
            ) : (
              <>
                <div>
                  <h2>¿Quiénes somos?</h2>
                  <p>True Fade es una tienda de ropa especializada en moda urbana y contemporánea. Nos dedicamos a ofrecer una selección curada de prendas y accesorios que reflejan las últimas tendencias del streetwear, manteniendo siempre un enfoque en la autenticidad, el estilo y la actitud.  <br><br>En True Fade, la ropa no es solo una prenda: es una declaración. Creemos que el estilo es una herramienta de expresión personal, un reflejo de actitud, cultura y autenticidad.</p>
                </div>
                <ImageMod
                  class="rounded-md mx-auto mt-10 lg:mt-0"
                  src={section.image}
                  width={536}
                  height={449}
                  alt={section.title}
                  format="png"
                />
              </>
            )}
          </div>
        ))
      }
    </div>
  </section>



  <section>
    <div class="container">
      <div class="text-center">
        <h2>Nuesto equipo</h2>

        <div
          class="grid sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6 mt-14"
        >
          {
            staff_section_enable &&
              staff!.map(
                (s: { name: string; designation: string; avatar: string }) => (
                  <div class="border border-border rounded-lg">
                    <div class="py-6 space-y-2">
                      <h3 class="h4">{s.name}</h3>
                      <p class="text-text-dark dark:text-darkmode-text-light">
                        {s.designation}
                      </p>
                    </div>
                    <div class="bg-light rounded-b-xl mx-auto">
                      <ImageMod
                        src={s.avatar}
                        alt={`Staff-${s.name}`}
                        width={290}
                        height={250}
                        class="mx-auto w-full h-[250px] rounded-b-xl overflow-hidden"
                      />
                    </div>
                  </div>
                )
              )
          }
        </div>
      </div>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <div
        class="bg-light px-7 py-20 dark:bg-darkmode-light text-center rounded-md"
      >
        <h2>¿Por qué nosotros?</h2>

        <div class="row justify-center gap-6 mt-14">
          <div class="col-6 md:col-5 lg:col-3">
            <div class="flex justify-center">
              <FaHeadset size={48} />
            </div>
            <h3 class="md:h4 mt-6 mb-4">24/7 Soporte amigable</h3>
            <p>Nuestro equipo de soporte siempre está listo para ti los 7 días de la semana</p>
          </div>

          <div class="col-6 md:col-5 lg:col-3">
            <div class="flex justify-center">
              <FaBoxOpen size={48} />
            </div>
            <h3 class="md:h4 mt-6 mb-4">7 Días de cambio sin costo</h3>
            <p>Reporta cualquier avería en un plazo de 7 días para un cambio inmediato.</p>
          </div>

          <div class="col-6 md:col-5 lg:col-3">
            <div class="flex justify-center">
              <FaRegCheckCircle size={48} />
            </div>
            <h3 class="md:h4 mt-6 mb-4">Calidad Garantizada</h3>
            <p>
              Si su producto no es perfecto, devuélvalo para obtener un reembolso completo
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>
{
    testimonials_section_enable && (
      <Testimonials
        client:load
        title={testimonials_section_title!}
        testimonials={testimonials!}
      />
    )
  }
  <section>
    <div class="container">
      <div
        class="bg-light px-7 lg:px-32 py-20 dark:bg-darkmode-light mb-14 xl:mb-28 rounded-md"
      >
        <div class="row">
          <div class="md:col-5 mx-auto space-y-5 mb-10 md:mb-0">
            <h1 set:html={markdownify(faq_section_title!)} />
            <p
              set:html={markdownify(faq_section_subtitle!)}
              class="md:text-lg"
            />

            {
              button?.enable && (
                <a
                  class="btn btn-sm md:btn-lg btn-primary font-medium"
                  href={button.link}
                >
                  {button.label}
                </a>
              )
            }
          </div>
          
          <div class="md:col-7">
            <Accordion client:load faqs={faqs} />
          </div>
        </div>
      </div>
    </div>
  </section>
</Base>
